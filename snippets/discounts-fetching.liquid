<div class="available-offers">
  <h3>Available Offers</h3>
  <ul>
    {% for block in section.blocks %}
      <li>
        <strong>{{ block.settings.coupon_code }}</strong> – {{ block.settings.coupon_desc }}
      </li>
    {% endfor %}
  </ul>
</div>

{% schema %}
{
  "name": "Available Offers",
  "settings": [],
  "blocks": [
    {
      "type": "coupon",
      "name": "Coupon",
      "settings": [
        {
          "type": "text",
          "id": "coupon_code",
          "label": "Coupon Code",
          "default": "DISCOUNT10"
        },
        {
          "type": "text",
          "id": "coupon_desc",
          "label": "Description",
          "default": "10% off your entire order"
        }
      ]
    }
  ]
}
{% endschema %}